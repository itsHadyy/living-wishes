# Supabase — copy this file to .env.local and fill in your values.
# Get URL and anon key from: Supabase Dashboard → Project Settings → API
#
# Create React App only exposes variables that start with REACT_APP_
# (If you migrate to Next.js, use NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY instead.)

REACT_APP_SUPABASE_URL=https://your-project-ref.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key-here
