# Edge Functions

This project uses Supabase for verification (magic link). No custom Edge Functions are required for the signup flow.

If you add new functions later, deploy with:

```bash
supabase functions deploy <function-name>
```
